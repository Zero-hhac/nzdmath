# 漏洞修复实施报告

> 依据《漏洞修复方案.md》实施，全部代码修复已完成并分批复提交（每批独立 commit），
> 每批通过 go build / go vet / go test / gofmt 与前端 tsc + 生产构建回归。

## 提交记录

| Commit | 批次 | 内容 |
|---|---|---|
| `1af0971` | 第一批 P0 + 第二批 P1 | #1 #2 #3 #22（连带 #9 #11 #13 #14 #15 #25 #28） |
| `4b8567e` | 第三批 P2 + M2 | #12 #17 #18 #19 #20（连带 M2 上传目录移出版本库） |
| `16c2208` | 第四批 P3 + M4 | #23 #24 #26 #27 + M4 强制初始密码环境变量 |

## 逐项修复明细

### 第一批 P0（commit 1af0971）
- **#1 注册用户名提权**：`backend/internal/service/user.go` 注册时新增管理员表同名检查与保留名黑名单（admin/administrator/root/system 等），提示统一为"用户名已存在"；登录逻辑删除"用户名等于 admin 即管理员"特判，身份只由角色字段与管理员表记录决定；启动时 `CleanupAdminShadowAccounts()` 清洗存量被抢注记录（密码哈希与角色同步为管理员表值）。
- **#2 私信全员广播**：`backend/internal/ws/hub.go` 新增 `SendToUsers` 定向推送；`direct_message_service.go` 四处发送方法（会员/管理员 × 文本/文件）全部改为向会话双方（发送方 + 接收方/管理员侧）投递，不再全员广播，direct 通道标记不变、前端零改动。
- **#3 私聊文件**：
  - 新建 `backend/internal/service/upload_validation.go` 公共校验模块（扩展名白名单 + 512 字节魔数嗅探），聊天室与私聊两上传入口统一接入，HTML/SVG 伪装文件被拒；
  - `/uploads/direct` 由静态挂载改为 `GET /uploads/direct/*filepath` 接口（`direct_file_handler.go`）：查询私信表校验归属，仅会话会员与管理员可下载，第三方 403；兼容用户 token 与 admin token；
  - **#22** 管理端 `DirectChatPanel.tsx` 改用 `authFetchBlob(url, 'admin')` + blob URL 展示/下载图片附件，修复全部 401。
- **#9 公开 HTML**：删除资源目录遗留 `1780920203464914000_index.html`；`router/static.go` 对 /uploads/resources 的 HTML/HTM/SVG 强制附件下载 + 改写 Content-Type，不再同源内联执行。
- **#13 下载限流**：`/resources/download/:id` 挂载 30 次/分钟 IP 限流。
- **#14 query token**：`resource_handler.go` 删除 query 传 token 兼容分支，前端 `api.ts` 同步移除。
- **#15 枚举/时序**：注册发码对已注册邮箱静默返回"已发送"；登录用户不存在/禁用时补一次等代价假 bcrypt 比较并统一提示"用户名或密码错误"。
- **#25/#28**：管理员自动建号失败不再签发 user_id=0 的 token；注册失败对外返回通用提示、细节入服务端日志。

### 第二批 P1（commit 1af0971）
- **#4 报名并发超卖**：`event_registration_service.go` 活动读取移入事务并对活动行 `SELECT ... FOR UPDATE`，名额/时间/过期判断全部基于锁后数据。
- **#5 token 吊销**：新建 `service/token_revoke.go`（用户→token 集合 Redis 索引，与白名单同 TTL）；改密/重置/封禁/删除六处调用统一吊销；登出同步移除索引；admin_token 有效期改读配置（不再硬编码 24h）。
- **#6 垂直越权**：`admin_user_service.go` 单用户重置密码/启停/删除统一拒绝管理员角色行（含用户名 admin），与批量接口对齐。
- **#7 注册验证码限次**：每邮箱失败计数，5 次后验证码作废要求重新获取。
- **#8 找回密码冷却**：按"用户名+邮箱"60 秒发送冷却；尝试计数只在生成新验证码时重置。

### 第三批 P2（commit 4b8567e）
- **#12 资源物理文件**：`admin_resource_service.go` 删除时同步删物理文件；下线（status 1→0）移入 `{upload}/archive/resources`，恢复上架移回，保证"下线即不可下载"。
- **#17 限流 fail-open**：`ratelimit.go` Redis 异常时降级进程内存滑动窗口兜底限流（阈值 ×4），不再完全放行。
- **#18 CSP**：`frontend/nginx.conf`、`nginx.ssl.conf`、`nginx.ssl.conf.example` 增加 CSP 响应头（script-src 'self'、object-src 'none' 等）。
- **#19 收藏占位**：`favorite_service.go` 仅回显 status=1 内容，已下线条目显示"内容已下架"占位。
- **#20 点赞/浏览量**：隐藏评论点赞被拒；作品浏览量按"IP+作品"Redis 24h 去重后再自增（ShowcaseService 注入 rdb，详情接口传 ClientIP）。

### 第四批 P3（commit 16c2208）
- **#23 email 语义**：`member_service.go` 更新资料 email 留空即不变，不再误清空。
- **#24 回复分页**：新增 `GET /comments/:id/replies` 分页接口（`ListReplies` + handler + 路由 + 前端 `getCommentReplies` API）。
- **#26 nginx WS**：`nginx.ssl.conf`/example 增加 `map $http_upgrade $connection_upgrade` + HTTP/1.1 + Upgrade/Connection 透传，读超时 3600s；`frontend/nginx.conf`（conf.d 场景）用 `$http_upgrade` 直接赋值实现同等回落效果。
- **#27 WS 限频**：`chat_ws_handler.go` 读循环滑动窗口限频（10 秒至多 10 条），超限推送 rate_warn 警告，连续 3 次超限断开连接。

### 运维遗留（部分完成）
- **M1** ✅ 已移除 git remote 中的明文 GitHub PAT（remote 改为 `https://github.com/Zero-hhac/nzdmath.git`）。⚠️ **仍需在 GitHub 上吊销该 token**（token 已从本文档移除，避免再次入版本库），它已出现在本地历史/配置中。
- **M2** ✅ 头像、资源上传目录已 `git rm --cached` 移出版本库跟踪，.gitignore 补齐规则并保留 h5_unified_light 固定 H5 目录跟踪。
- **M3** ❌ 未执行（需人工）：git 历史中仍含会员数据与上传文件，按《修复步骤.md》P0-C 用 filter-repo 重写历史或删库重建（破坏性操作，需用户确认后执行）。
- **M4** ✅ 代码部分完成：`EnsureDefaultAdmin` 任何模式（含 debug）都强制 `ADMIN_INITIAL_PASSWORD`，杜绝 admin/admin123；部署时需在 .env 与 docker-compose 中提供该变量。

## 待用户执行的运维动作
1. **#21 凭据轮换**：`backend/configs/config.yaml`（本地未入库）含真实 SMTP 授权码（15677578521@163.com）与弱数据库密码 `123456`。请在 163 邮箱后台重置授权码并更新配置；本地 MySQL 更换强密码。
2. **M1 吊销 GitHub PAT**：在 GitHub Settings → Developer settings → Personal access tokens 吊销该 PAT（完整 token 见 GitHub 后台，此处不再展示）。
3. **M3 历史重写**：确认后执行 filter-repo（或删库重建），清除历史中的会员数据与上传文件。
4. **M4 部署变量**：生产/本地启动均需设置 `ADMIN_INITIAL_PASSWORD`（强度需满足字母+数字），否则后端拒绝启动。
5. 部署 nginx 时应用更新后的 `nginx.ssl.conf`（WS 透传 + CSP）并 `nginx -t` 验证。

## 回归验证结果
- 后端：`go build ./...` ✅、`go vet ./...` ✅、`go test ./...` 全包通过 ✅、gofmt ✅
- 前端：`tsc --noEmit` ✅、`vite build` 生产构建 ✅
- 既有测试（路由鉴权、上传校验、hub 广播等）均未回归。

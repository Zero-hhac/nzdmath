# 上线前修改方案

> 适用范围：nzdmath（数学协会全栈项目）部署至公网前
> 优先级：P0 = 必须修改（不改不能上线）；P1 = 强烈建议；P2 = 可选优化

---

## P0-1 生产模式未开启（debug 泄漏调试信息）

**现状**：`backend/configs/config.docker.yaml:4` 写死 `mode: debug`，`docker-compose.yml` 只注入了 `APP_ENV=docker`，未注入 `APP_MODE`，所以 Docker 部署实际跑在 debug 模式。

**风险**：Gin debug 模式输出调试日志、异常堆栈信息，性能较差。

**修改**：
1. `docker-compose.yml` 与 `docker-compose.prod.yml.example` 的 backend 环境变量中增加：
```yaml
      - APP_MODE=release
```
2. 建议同时把 `config.docker.yaml` 的 `mode` 改为 `release`（双保险，环境变量会覆盖它）。

**验证**：`docker compose logs backend` 启动日志中不再出现 `[GIN-debug]` 路由表输出。

---

## P0-2 自动注入测试数据（生产环境定时炸弹）

**现状**：`backend/internal/router/router.go:66-67` 无条件调用 `seedSvc.AutoSeed()`。空库启动时会自动创建：
- 3 个测试会员：`testuser` / `math_geek` / `EulerFan`，密码均为 `password123`
- 大量假新闻、活动、资源数据
- 6 天假流量统计数据

**风险**：生产环境上线即自带弱口令账号和脏数据，攻击者可用 `password123` 直接登录。

**修改**：加开关，默认不注入。`router.go` 中改为：

```go
	// 注入初始测试数据：仅当显式设置 SEED_DEMO_DATA=true 时执行（生产环境不设置）
	if os.Getenv("SEED_DEMO_DATA") == "true" {
		seedSvc := service.NewSeedService(db)
		seedSvc.AutoSeed()
	}
```

需在文件顶部 import 中加 `"os"`。docker-compose 中**不要**设置 `SEED_DEMO_DATA`。

**验证**：空库启动后数据库中 `users` 表无测试账号；手动 `SEED_DEMO_DATA=true` 本地启动可正常注入（留给演示用）。

---

## P0-3 默认管理员弱口令 + 初始化失败静默

**现状**：`backend/internal/service/admin_service.go:41-45`：非 release 模式下密码为空时创建 `admin/admin123`。由于 P0-1 修复前 mode 是 debug，实际会创建弱口令管理员。且 `router.go:61-63` 初始化失败只 `slog.Warn`，不阻断启动——生产环境管理员创建失败会导致后台无法登录且无人知晓。

**修改**：
1. `admin_service.go` 中增加弱口令拦截（release 模式已有"必须设置密码"检查，再加强度检查）：
```go
	if len(password) < 10 {
		return errors.New("初始管理员密码长度不能少于 10 位")
	}
	if password == "admin123" || password == "password123" || password == "123456" {
		return errors.New("初始管理员密码过弱，请使用高强度密码")
	}
```
2. `router.go` 中初始化失败改为终止启动：
```go
	if err := adminInitSvc.EnsureDefaultAdmin(); err != nil {
		slog.Error("创建默认管理员失败，服务终止", "err", err)
		panic(err)
	}
```
3. 部署时在服务器 `.env` 中设置强 `ADMIN_INITIAL_PASSWORD`（见 P0-5）。

**验证**：不带 `ADMIN_INITIAL_PASSWORD` 且 `APP_MODE=release` 启动时容器应报错退出（而不是静默运行）。

---

## P0-4 MySQL / Redis 端口暴露公网

**现状**：`docker-compose.yml:13,28` 将宿主 `3307`/`6380` 绑定到 `0.0.0.0`。

**风险**：如果 ECS 安全组放行了这些端口，数据库直接对公网开放。

**修改**：正式部署改用生产编排文件（复制 `docker-compose.prod.yml.example` 为 `docker-compose.prod.yml`），其中已绑定 `127.0.0.1`；更彻底的做法是**直接删除 mysql/redis 的 `ports` 段**——同网络内的容器互访不需要端口映射，宿主机调试用 `docker exec -it math-top-mysql mysql` 即可。

**验证**：服务器上 `ss -tlnp | grep -E '3307|6380'` 无结果（或仅监听 127.0.0.1）；ECS 安全组只开放 80/443。

---

## P0-5 密钥管理

**现状**：`.env.example` 全部是占位符，需要真实强随机值。

**修改**：在**服务器**上生成三个密钥写入 `.env`（该文件已被 gitignore，勿提交、勿通过聊天工具传递明文）：

```bash
# 服务器上执行，各生成一次
openssl rand -base64 32   # MYSQL_PASSWORD
openssl rand -base64 32   # JWT_SECRET
openssl rand -base64 32   # ADMIN_INITIAL_PASSWORD
```

```bash
# /root/nzdmath/.env
MYSQL_PASSWORD=上面生成的值1
JWT_SECRET=上面生成的值2
ADMIN_INITIAL_PASSWORD=上面生成的值3
```

**验证**：`docker compose config` 能正常解析；`.env` 文件权限建议 `chmod 600`。

---

## P0-6 用户数据导出文件入库风险

**现状**：`backend/export_all.xlsx`、`export_check_all.xlsx`、`export_dept.xlsx` 三份含真实会员数据的文件裸放在项目目录，未纳入 `.gitignore`。

**修改**：
1. 删除这三个文件（数据可从后台重新导出）。
2. `.gitignore` 增加：
```
backend/export_*.xlsx
```

**验证**：`git status` 干净，`git ls-files | grep export` 无结果。

---

## P0-7 CORS 配置非法（既无保护也不工作）

**现状**：`backend/internal/middleware/cors.go:7,11` 同时设置 `Access-Control-Allow-Origin: *` 与 `Access-Control-Allow-Credentials: true`。这是浏览器规范禁止的组合：浏览器会直接拒绝跨域响应，且 `*` 意味着不设防。

**修改**：**方案 A（推荐，改动最小）**——前端经 Nginx 同域反代访问，不存在跨域，直接移除该中间件：
- `router.go:29` 删除 `r.Use(middleware.CorsMiddleware())`
- 删除 `cors.go` 文件

**方案 B（未来前端独立域名部署时再用）**——白名单：
```go
	origin := c.GetHeader("Origin")
	allowed := os.Getenv("CORS_ALLOWED_ORIGIN") // 如 https://www.你的域名.com
	if origin == allowed {
		c.Header("Access-Control-Allow-Origin", allowed)
		c.Header("Access-Control-Allow-Credentials", "true")
	}
```

**验证**：curl 请求不再返回 `Access-Control-Allow-Origin: *`。

---

## P1-1 限流覆盖面

**现状**：仅注册/登录/后台登录有 `RateLimitMiddleware`。上传、评论、聊天、`/active/track` 全部裸奔；`/active/track` 无鉴权，可被脚本刷爆 Redis 和统计。

**修改**：`router.go` 中给高频接口加限流：

```go
	commentLimit := middleware.RateLimitMiddleware(rdb, 10, time.Minute)  // 评论
	uploadLimit  := middleware.RateLimitMiddleware(rdb, 20, time.Hour)   // 上传
	trackLimit   := middleware.RateLimitMiddleware(rdb, 60, time.Minute) // 页面统计

	public.POST("/active/track", trackLimit, func(c *gin.Context) {...})
	auth.POST("/comments", commentLimit, commentHandler.Create)
	auth.POST("/chat/messages", middleware.RateLimitMiddleware(rdb, 30, time.Minute), chatHandler.SendText)
```

**验证**：连续高频请求触发 429。

---

## P1-2 文件上传安全加固

**现状**：`avatar_service.go` / `resource_service.go` 只校验扩展名，不校验文件真实内容（magic bytes），可上传伪装文件；资源还允许 `.js/.py/.ts/.json`，配合下载存在风险。

**修改**：
1. 读取文件前 512 字节用 `net/http.DetectContentType` 校验，与允许的扩展名/类型白名单比对，不匹配即拒绝。
2. 从资源白名单中移除 `.py/.js/.ts` 等可执行/源码类型（如确有需求保留，确保下载时 `Content-Disposition: attachment`）。
3. **下载路径穿越修复**（`resource_service.go:97-103`）：DB 中的 `FilePath` 不可信，需规范化并校验前缀：

```go
	uploadBase := config.GlobalConfig.Storage.UploadDir
	if uploadBase == "" {
		uploadBase = "./storage/uploads"
	}
	baseAbs, _ := filepath.Abs(uploadBase)
	full := filepath.Join(baseAbs, strings.TrimPrefix(resource.FilePath, "/uploads/"))
	full = filepath.Clean(full)
	if !strings.HasPrefix(full, baseAbs+string(os.PathSeparator)) {
		return nil, errors.New("非法文件路径")
	}
	resource.FilePath = full
```

**验证**：构造 `FilePath` 为 `/uploads/../../etc/passwd` 的记录，下载返回错误而非文件内容。

---

## P1-3 后端健康检查与依赖顺序

**现状**：backend 无 healthcheck，frontend 仅 `service_started` 依赖，可能出现前端先于后端就绪启动（影响不大，但可加固）。

**修改**：`docker-compose.prod.yml` 中 backend 增加：
```yaml
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 20s
```
frontend 依赖改为：
```yaml
    depends_on:
      backend:
        condition: service_healthy
```

---

## P1-4 SSL / Nginx 加固与证书自动化

**现状**：`nginx.ssl.conf` 使用测试域名 `hahahahaha.icu`，证书靠手动放置 `server.pem/server.key`，无自动续期。

**修改**：
1. 将 `server_name` 替换为正式域名。
2. 证书改用 **Certbot/ACME** 自动签发续期（推荐宿主机安装 certbot，用 `--webroot` 或 DNS 验证，每 90 天自动续）。
3. `nginx.ssl.conf` 的 443 server 块补充：
```nginx
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    ssl_session_cache shared:SSL:10m;
    ssl_stapling on;
    ssl_stapling_verify on;
```

**验证**：[SSL Labs](https://www.ssllabs.com/ssltest/) 评分 A 以上。

---

## P1-5 前端死代码与仓库杂物清理

**现状**：`@google/genai`（未使用）、`dotenv`、`express` 装在 dependencies；`vite.config.ts:12-14` 把 `GEMINI_API_KEY` 注入前端 bundle（一旦填了真实 key 会泄露给所有访客）；`a.html`（Tailwind CDN 原型稿）、`scratch/`、`server.exe` 等杂物。

**修改**：
1. `vite.config.ts` 删除 `define` 块（保留 loadEnv 给 VITE_BACKEND_PORT 用）。
2. `frontend/.env.example` 删除 `GEMINI_API_KEY`。
3. 卸载未用依赖并验证构建：
```bash
npm uninstall @google/genai dotenv express
npm run lint && npm run build
```
4. 删除 `frontend/a.html`、`frontend/scratch/`、`backend/server.exe`（`git rm` 已跟踪项）。

---

## P1-6 备份与日志轮转

**现状**：MySQL 无备份、容器日志无大小限制（磁盘可被撑爆）。

**修改**：
1. **数据库每日备份**（服务器 crontab）：
```bash
mkdir -p /root/backups
crontab -e
# 每天凌晨 3 点备份并保留 14 天
0 3 * * * docker exec math-top-mysql sh -c 'mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" math_top' | gzip > /root/backups/math_top_$(date +\%F).sql.gz && find /root/backups -name '*.gz' -mtime +14 -delete
```
2. **日志轮转**：docker-compose 各服务加：
```yaml
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
```

**验证**：手动执行备份命令可得到非空 sql.gz，且能还原到测试库。

---

## P2 可选优化（不阻塞上线）

- **迁移管理**：生产环境用 golang-migrate 等显式迁移替代启动时 `AutoMigrate`。
- **CI 增强**：`.github/workflows/ci.yml` 增加 `golangci-lint run`、`govulncheck ./...`、`npm audit`。
- **监控告警**：接入 Uptime Kuma / 阿里云云监控，探测 `/health`。
- **测试补充**：handler 层与 middleware 层单元测试。
- **Redis 持久化**：如不能接受重启后全员登出（token 存 Redis），配置 AOF。

---

## 上线前检查清单

- [ ] `.env` 三个密钥均为强随机值，文件权限 600
- [ ] `APP_MODE=release` 已注入
- [ ] `SEED_DEMO_DATA` 未设置（确认库中无 `testuser` 等账号）
- [ ] 弱口令拦截生效，管理员密码已设置且未使用默认值
- [ ] 3306/6379 未暴露公网，ECS 安全组仅开放 80/443
- [ ] `export_*.xlsx` 已删除并加入 .gitignore
- [ ] CORS 中间件已移除（或白名单化）
- [ ] SSL 证书已签发并配置自动续期，HSTS 已启用
- [ ] 备份 cron 已配置并完成一次恢复演练
- [ ] 限流已覆盖注册/登录/评论/聊天/上传/统计
- [ ] 上传文件 magic bytes 校验、下载路径穿越已修复
- [ ] 前端死依赖与杂物已清理，`npm run lint && npm run build` 通过
- [ ] `go test ./...` 与 `go vet ./...` 通过

## 建议上线顺序

1. 按 P0-1 ~ P0-7 改代码 → 本地 `docker compose up --build` 完整回归
2. 服务器安装 Docker + 镜像加速器（阿里云 ACR）
3. 上传代码，复制 `docker-compose.prod.yml`，写入 `.env` 与 SSL 配置
4. ECS 安全组只开 80/443，域名解析
5. `docker compose -f docker-compose.prod.yml up -d --build`
6. 按检查清单逐项验收

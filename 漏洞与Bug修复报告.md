# 漏洞与 Bug 修复报告

> 范围：本项目近八轮整改（代码审查 → 黑盒渗透 → 三轮安全整改 → 三大功能升级 → WS 实时推送）中
> **发现并修复**的全部问题，以及**发现但未处理**的遗留项。
> 对应 git 提交与查看方式见文末「六、对应 git 提交」。

---

## 总览

| 类别 | 数量 | 说明 |
|---|---|---|
| 安全漏洞 | 12 | 高危 2 · 中危 6 · 低危 4 |
| 功能 Bug | 8 | 日期口径、数据丢失、WS 断连、UI 缺陷等 |
| 工程/运维问题 | 8 | 路由冲突、备份缺失、产物入库等 |
| 遗留未处理 | 3+ | PAT 明文、git 历史会员数据、dev 默认口令、测试覆盖率等 |

---

## 一、安全漏洞（12 个）

| # | 漏洞 | 严重度 | 发现方式 | 解决方案 | 修复后验证 |
|---|---|---|---|---|---|
| S1 | **X-Forwarded-For 伪造可绕过全站限流 + 污染 UV 统计** | 🔴 高危 | 黑盒对照实验：固定 XFF 第 11 次登录触发 429；轮换 XFF 连续 15 次全部放行 | gin 默认信任所有代理（0.0.0.0/0），`ClientIP()` 直接采信 XFF 最左值。在 router 中 `SetTrustedProxies(docker 网段)`，XFF 改为从右往左取首个非可信 IP，本地/公网直连的 XFF 一律忽略 | 修复后轮换 XFF 连续 12 次，第 9 次起 429，绕过失效 ✅ |
| S2 | **聊天室文件未授权即可下载** | 🔴 高危 | 黑盒：匿名 curl 直接下载 `/uploads/chat/...` 成功（修复前） | `/uploads` 不再整根公开挂载，改为目录白名单（avatars/covers/h5_unified_light/resources），chat 目录单独挂 JWT 鉴权静态路由；前端图片/文件改认证 fetch + blob URL；新增路由单测（最初的"影子路由"方案在 gin 下会启动 panic，已被单测证实并改为此方案） | 匿名/伪造 token → 401；合法 token → 200；目录列举与路径穿越（裸 `../`、`%2e%2e`）→ 404 ✅ |
| S3 | **密码策略过弱（会员与管理员不一致）** | 🟠 中危 | 黑盒：`123456` 注册成功；管理员初始密码反而要求 ≥10 位 | 新增 `utils/password.go` 统一策略（6-72 位且必须含字母+数字），接入注册/改密/管理员初始密码/管理员自助改密/后台重置（单个+批量）五个入口；前端三处表单同步校验与提示 | 纯数字 400、纯字母 400、`abc123` 通过 ✅ |
| S4 | **请求体大小无限制（内存 DoS）** | 🟠 中危 | 黑盒：5MB JSON 直通解析 | 新增 `BodyLimitMiddleware`（JSON 1MB / multipart 64MB）挂全局；超限由各 handler 统一转 400 | 2MB JSON → 400；头像 multipart 上传不受影响 ✅ |
| S5 | **评论无长度上限** | 🟠 中危 | 黑盒：20000 字符评论成功入库 | `CommentService.Create` 增加 2000 字上限 | 3000 字 → 400 ✅ |
| S6 | **收藏 target_type 零校验 + 可无限重复收藏** | 🟠 中危 | 黑盒：`"anything<script>"` 成功入库 | 类型白名单（event/news/resource/showcase）+ 去重检查 + 哨兵错误 | 非法类型/重复收藏 → 400 ✅ |
| S7 | **资料更新无校验（昵称/简介/邮箱/头像任意字符串）** | 🟠 中危 | 代码审计 | 昵称 ≤50 字、简介 ≤2000 字、邮箱格式校验、头像仅限本站 `/uploads/avatars/` 路径或 http(s) URL 且 ≤500 字符 | 非法输入 → 400 ✅ |
| S8 | **越权操作返回码错误** | 🟡 低危 | 黑盒：删除他人收藏返回 500、删除他人评论返回 400 | 评论/收藏服务改用哨兵错误，handler 映射：越权 403、不存在 404 | 越权删除评论/收藏均 → 403 ✅ |
| S9 | **后台用户列表 pageSize 无上限** | 🟡 低危 | 代码审计 | 截断为 100 | ✅ |
| S10 | **DB 错误细节透传给客户端** | 🟡 低危 | 代码审计 | 收藏/评论/资料更新等 handler 改为通用提示，校验类错误用哨兵错误区分 | ✅ |
| S11 | **Markdown 渲染放开 `style` 属性（CSS 注入）** | 🟡 低危 | 代码审计 | 移除通配 `style` 放行，仅保留 `className` | ✅ |
| S12 | **dev 环境默认管理员 admin/admin123** | 🟠 中危 | 黑盒：默认口令登录成功 | release 模式已有"必须设置 ADMIN_INITIAL_PASSWORD + 弱口令黑名单"强制校验；后台新增「账号设置」自助改密入口。**本地 debug 模式默认口令仍然存在（见遗留 R3）** | 管理员改密闭环实测通过 ✅ |

---

## 二、功能 Bug（8 个）

| # | Bug | 严重度 | 发现方式 | 解决方案 | 验证 |
|---|---|---|---|---|---|
| B1 | **仪表盘"今日"统计按 UTC 零点截断**，东八区每天 0-8 点数据算错天，且与 Redis 日指标键（本地日期）口径不一致 | 🔴 高 | 代码审查 | 改用本地时区零点 `time.Date(...)`，两套口径统一 | 代码复核 + 全量回归 ✅ |
| B2 | **PV/UV 指标归档依赖管理员打开后台**，Redis 日 key 48h 过期后数据永久丢失 | 🔴 高 | 代码审查 | `syncDailyMetrics` 导出为 `SyncDailyMetrics` 并常驻化（启动补档一次 + 每小时 ticker）；日 key TTL 延长至 8 天 | 日志与补档实测 ✅ |
| B3 | **Dashboard 全部 Count/Scan/Redis 错误被吞**，DB 故障时静默显示 0 | 🟠 中 | 代码审查 | 逐处检查错误并返回 | ✅ |
| B4 | **开发模式 WebSocket 全部 403**：vite 代理 `changeOrigin:true` 把 Host 改成 8080，后端同源校验（Origin vs Host）失败——聊天室/通知推送在 dev 一直连不上 | 🔴 高 | 黑盒：Python 走 3000 代理握手实测 403 | vite 代理改为 `changeOrigin:false` | 代理握手 101；浏览器角标实时推送实测 ✅ |
| B5 | **通知弹层"关不掉"**：AnimatePresence 直接子元素不是 motion 组件，退出动画永不完成，不可见幽灵节点残留在 DOM | 🟠 中 | 浏览器实测（X 点击后节点仍在，opacity 已为 0） | 改为条件渲染 + 仅入场动画，关闭即刻卸载；补充 X 按钮、遮罩点击、Escape、外点四路关闭 | X 关闭后 DOM 完全移除、外点关闭 ✅ |
| B6 | **移动端通知弹层溢出被裁切**，遮罩只能盖住导航栏区域（transform 祖先劫持 fixed 定位） | 🟠 中 | 浏览器 375×667 几何测量 | `createPortal` 渲染到 body + `fixed` 水平居中（`w-[calc(100vw-1.5rem)] max-w-[26rem]`）+ 全屏遮罩 | 面板完整可见、居中偏差 0px、遮罩覆盖全屏 ✅ |
| B7 | **活动报名行随详情内容滚动** | 🟡 低 | 用户反馈 + 浏览器实测 | `OverlayConfig` 新增固定 `footer` 区，报名栏移入卡片底部 | 滚动前后 footer Y 坐标 321→321 不变 ✅ |
| B8 | **活动标记过期后直接消失**（预期是可见并标注） | 🟡 低 | 用户反馈 | 公开列表保留过期活动（排序垫底），卡片轻灰 + 「已过期」角标，详情与报名接口拒绝报名，首页仍排除 | 列表可见带角标、报名 400、恢复后回归 ✅ |

---

## 三、工程/运维问题（8 个）

| # | 问题 | 解决方案 |
|---|---|---|
| E1 | **gin 通配路由冲突**：`/uploads/chat/*filepath` 与 `/uploads/*filepath` 同挂会启动 panic（影子路由方案不可行） | 改为按目录白名单挂载，并新增 `chat_static_test.go` 单测锁死该约束 |
| E2 | compose 使用已弃用的 `version: '3.8'` | 删除两个 compose 文件的 version 字段 |
| E3 | README 存在 Mac 绝对路径死链、"命名卷"表述与实际 bind mount 不符、`package.json` 名为 react-example 且依赖多余 `@types/express` | 逐一修正并对齐文档与配置 |
| E4 | **uploads 目录无备份**（数据库有备份脚本） | `scripts/backup.sh` 增加 uploads 打包 + 同策略清理 |
| E5 | **构建产物误入版本库**（`backend/server` 二进制、`frontend/dist` 等） | 移除跟踪 + .gitignore 覆盖；`backend/server` 在 main 上单独提交移除（4f3e901）。历史中的旧产物仍存在（见遗留） |
| E6 | 部分文件不符合 gofmt | 本轮改动文件全部 gofmt（历史文件遗留若干） |
| E7 | **测试缺口** | 新增路由鉴权单测、限流单测、WS Hub 单测等 7 个测试文件；前端仍无测试（见遗留） |
| E8 | 通知红点非实时（仅路由切换刷新） | 新增 `/api/v1/ws/notify` 推送通道（与聊天室同 Hub、独立鉴权），管理员发通知/报名成功自动通知即时推送 |

---

## 四、遗留未处理项（诚实清单）

| # | 事项 | 风险 | 建议 |
|---|---|---|---|
| R1 | **`git remote` 中明文 GitHub PAT**（`https://ghp_...@github.com/...`） | 拿到 .git/config 即可冒用推送权限 | 吊销该 token，remote 换 SSH 或 `gh auth setup-git` |
| R2 | **真实会员数据仍在 git 历史**（commit `67bf563` force-add 的头像/简历/名单 xlsx） | 仓库公开即全员信息泄露 | 按 `修复步骤.md` P0-C：git filter-repo 重写历史或删库重建 |
| R3 | **本地 debug 模式默认管理员 admin/admin123** | 局域网内可登后台 | 用后台「账号设置」改掉；或本地开发也设 ADMIN_INITIAL_PASSWORD |
| R4 | 测试覆盖率仍薄（前端 0 测试，业务 service 裸奔）；AutoMigrate 无版本化；WS Hub 单实例；容器以 root 运行 | 工程质量/扩容隐患 | 见 `技术栈报告` 演进建议 |

---

## 五、验证方法总述

- **静态**：代码审查、`go vet`、`gofmt`、`tsc --noEmit`
- **单测/构建**：`go test ./...`（含新增路由鉴权/限流/Hub 单测）、`vite build`、双镜像 docker build（CI）
- **黑盒**：curl 对照实验（限流 XFF 绕过前后对比）、Python 原始 WebSocket 握手与帧接收、路径穿越/注入/越权/上传伪装探测
- **真实浏览器**：Kimi WebBridge 实测——375×667 移动视口几何测量（居中偏差 0px、遮罩覆盖全屏、footer 固定）、实时推送角标（发通知 3 秒内 2→3，零页面交互）
- **数据卫生**：所有测试账号（pentest_*/fwtest_*/pwtest_*/uptest_*/nttest_*/exptest_*/belltest_*/wstest_*）、测试活动、测试通知、上传残留均已清理

---

## 六、对应 git 提交

| Commit | 内容 | 查看方式 |
|---|---|---|
| `1b251dd` | 后端：七轮安全整改 + 三大功能升级 | `git show 1b251dd` |
| `d518f0d` | 前端：红点/找回密码/报名栏/过期态/后台面板/移动端修复 | `git show d518f0d` |
| `83507c5` | 文档运维：部署文档、备份脚本、修复手册 | `git show 83507c5` |
| `4f3e901` | 移除误入版本库的构建产物 backend/server | `git show 4f3e901` |
| `2b70269` | 合并进 main 主线（--no-ff） | `git show 2b70269` |
| `a21306d` | 修复手册补录第七/八轮记录 | `git show a21306d` |

完整过程日志见根目录 `修复步骤.md`（第一~八轮全部记录），渗透测试详情见其「第二轮实测发现」章节。

# 二期整改方案

> 范围：针对最新一轮代码审查发现的剩余问题
> 优先级：P0 = 上线前建议完成；P1 = 上线后第一轮迭代；P2 = 可选

---

## P0-1 前端引入真实路由（当前最大短板）

**现状**：`frontend/src/App.tsx` 用 `useState<TabId>` 切换视图，无 URL。刷新回首页、无法分享链接、浏览器后退失效；nginx 已配置的 SPA `try_files` 因此形同虚设。

**方案**：引入 `react-router-dom`（React 19 兼容），URL 驱动导航。

1. 安装：`npm install react-router-dom`
2. TabId → 路径映射：

| Tab | 路径 |
|---|---|
| home | `/` |
| events | `/events` |
| resources | `/resources` |
| showcase | `/showcase` |
| news | `/news` |
| about | `/about` |
| chat | `/chat` |
| portal | `/portal` |
| admin | `/admin` |

3. 改造要点：
   - `App.tsx`：`BrowserRouter` + `<Routes>` 声明路由，保留现有 `React.lazy` 懒加载和 `Suspense`；路由切换滚顶逻辑移到 `useLocation` 监听；加 `*` 兜底路由重定向首页
   - `Navbar.tsx`：`setActiveTab` 改为 `<Link>`/`NavLink`（`NavLink` 自带激活态样式，可删掉 `activeTab` prop）
   - 各 view 的 `navigate` prop：保留兼容（组件内部仍可用），但新增页面跳转改用 `useNavigate()`
   - `AppOverlay`（登录弹窗等）不变，仍是全局状态
4. 影响面：`App.tsx`、`Navbar.tsx`、所有 view 中调用 `navigate` 的位置（需逐个 grep 确认，预计十几个调用点）
5. 验证：直接访问 `/chat`、`/admin` 等路径刷新页面可正常渲染；分享链接可直达。

**注意**：前端 `activeTab` 状态目前在多个组件间传递，改造时建议顺手删掉这套 prop drilling，全部走 URL。

---

## P0-2 WebSocket 握手安全口子

**现状**：`backend/internal/handler/chat_ws_handler.go`：
- `CheckOrigin: func(r *http.Request) bool { return true }` —— 不校验来源
- 降级方案 `?token=` 查询参数 —— token 会进入 access log、反向代理日志、浏览器历史

**方案**：
1. 删除查询参数降级（子协议方案已工作，且前端 `lib/ws.ts` 已实现）：
```go
	if token == "" {
		token = c.Query("token")   // ← 删除这两行
	}
```
2. CheckOrigin 校验同源（保留无 Origin 头的非浏览器客户端，如 Python 脚本）：
```go
	CheckOrigin: func(r *http.Request) bool {
		origin := r.Header.Get("Origin")
		if origin == "" {
			return true
		}
		u, err := url.Parse(origin)
		if err != nil {
			return false
		}
		return strings.EqualFold(u.Host, r.Host)
	},
```
3. 验证：带伪造 Origin 的 WS 握手被拒；浏览器正常连接不受影响。

---

## P0-3 聊天文件白名单与资源上传统一

**现状**：资源上传已移除 `.py/.js/.ts`，但 `chat_service.go:45` 的 `chatFileExts` 仍保留这三个扩展名。

**方案**：`chatFileExts` 移除 `.py`、`.js`、`.ts`，与资源白名单保持一致。

---

## P0-4 DAU 统计逻辑迁出路由层

**现状**：`router.go:134-177` 有 40 行匿名函数在路由注册处做 PV/UV/DAU 统计，违反项目自身分层。

**方案**：新建 `backend/internal/handler/activity_handler.go` + `backend/internal/service/activity_service.go`：

```go
// service
type ActivityService struct { rdb *redis.Client }

func (s *ActivityService) Track(ctx context.Context, clientIP, tokenString string) error {
    // PV 计数、UV 按 IP PFAdd、DAU 按 userID PFAdd（token 解析移入此方法）
}

// handler
func (h *ActivityHandler) Track(c *gin.Context) { ... }

// router 中简化为一行
public.POST("/active/track", trackLimit, activityHandler.Track)
```

---

## P1-1 AdminView.tsx 拆分（42KB 单体）

**现状**：整个后台管理塞在一个文件里，前端全项目最大文件。

**方案**：按模块拆分，`AdminView.tsx` 退化为"容器 + 侧边栏 + 面板渲染"：

```
frontend/src/views/admin/
├── AdminView.tsx            # 容器：登录态检查 + 面板切换 + 布局
├── DashboardPanel.tsx       # 统计仪表盘
├── EventsPanel.tsx          # 活动管理
├── NewsPanel.tsx            # 资讯管理
├── ResourcesPanel.tsx       # 资源管理
├── ShowcasesPanel.tsx       # 作品管理
├── UsersPanel.tsx           # 会员管理（含批量操作）
├── CommentsPanel.tsx        # 评论管理
└── ChatPanel.tsx            # 聊天管理
```

共享的表格/弹窗样式抽到 `views/admin/shared.tsx` 或复用现有组件。拆一个模块、构建通过一次，逐步推进，避免一次性大爆炸。

---

## P1-2 测试补齐（重点补 WS 与校验逻辑）

**现状**：只有 3 个 service 测试文件；新增的 `ws/hub.go`（并发代码）、限流、上传校验、生产配置校验零测试。

**方案**（优先可无外部依赖的）：
1. `ws/hub_test.go`：Register/Unregister、广播送达、慢客户端丢帧不阻塞、Unregister 关闭 Send
2. `service/resource_service_test.go` 补充 `validateUploadContent`：伪装 HTML 的 .jpg 拒绝、真 PNG 通过、空文件处理
3. `config/config_test.go` 补充 `validateProductionConfig`：弱 secret panic、空密码 panic
4. `middleware/ratelimit_test.go`：需 `github.com/alicebob/miniredis/v2`（纯内存假 Redis，dev 依赖）——限流计数、超限 429、Redis 挂时放行
5. `service/admin_user_service_test.go`：批量操作参数校验（空 ids、弱密码）——用 sqlite 内存库即可覆盖 SQL 逻辑，`go-sqlite3` 需 CGO；或用 gorm mysql 连接测试环境跳过，先只测纯校验分支

**验证**：`go test ./...` 覆盖率对比改造前有实质提升（可 `-cover` 输出）。

---

## P1-3 运维配套落地

1. **迁移管理**（短期方案）：保留启动 AutoMigrate，但在 release 模式打印显眼警告日志，提醒新增字段需人工确认；中期可切换 golang-migrate。
2. **数据库备份**：新增 `scripts/backup.sh`（mysqldump + gzip + 保留 14 天），服务器 crontab 每日 3 点执行；上线后做一次恢复演练。
3. **监控**：docker 起一个 Uptime Kuma 容器探测 `https://域名/health` 与首页，掉线推送（钉钉/邮件）；ECS 云监控配置 CPU/内存告警。
4. **HTTPS 证书自动化**：certbot（`--webroot` 或 DNS 验证）+ crontab 自动续期，替换手动放 `server.pem` 的做法。

---

## P1-4 前端 chunk 拆分

**现状**：vite build 警告 `index.js` 651KB、`ResourcesView` 540KB、`AdminView` 411KB（KaTeX、图表、markdown 全家桶）。

**方案**：`vite.config.ts` 增加 `manualChunks` 把重型库单独分包（KaTeX 会被多个页面用，拆出后可共享缓存）：

```ts
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        katex: ['katex', 'rehype-katex', 'remark-math'],
        charts: ['recharts'],
        markdown: ['react-markdown', 'remark-gfm', 'rehype-raw', 'rehype-sanitize'],
      },
    },
  },
},
```

上线后用 Lighthouse 跑一次首屏，看 LCP 是否达标。

---

## P2 可选

- CI 增强：`golangci-lint run`、`govulncheck ./...`、`npm audit --audit-level=high` 加入 `.github/workflows/ci.yml`
- 统一错误响应语义：部分 handler 把业务错误（如"资源不存在"）也返回 500，建议梳理 400/404/500 边界
- WS 多实例支持：`ws/hub.go` 注释已预留 Redis Pub/Sub 方案，若未来横向扩容再落地
- 前端 `navigate` prop 全面替换为路由后，清理所有 prop drilling

---

## 实施顺序建议

1. **本轮（上线前）**：P0-1 路由 → P0-2 WS 口子 → P0-3 白名单 → P0-4 DAU 迁移，每项完成后 `go build/vet/test` + `npm run lint/build` 全绿再进下一项
2. **上线后第一轮**：P1-1 AdminView 拆分（可与路由改造并行分步做）→ P1-2 测试 → P1-3 运维 → P1-4 分包
3. P2 按需
